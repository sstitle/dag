[package]
name = "dag-core"
version = "0.1.0"
edition = "2021"
license.workspace = true

[features]
serde = ["dep:serde", "slotmap/serde"]

[dependencies]
slotmap = "1"
thiserror = "1"
serde = { version = "1", optional = true }

[dev-dependencies]
serde_json = "1"
