[workspace]
members = [
    "core",
    "bindings/python",
    "bindings/node",
]
default-members = ["core"]
resolver = "2"

[workspace.package]
license = "MIT OR Apache-2.0"

[workspace.dependencies]
dag-core = { path = "core" }
serde_json = "1"
